{
  "name": "zoho-mail-mcp",
  "version": "2.0.0",
  "description": "Remote Zoho Mail MCP server (Cloudflare Workers) — per-user OAuth, read/search/draft/send email from Claude",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types"
  },
  "keywords": ["mcp", "zoho", "mail", "email", "claude", "ai", "cloudflare", "remote"],
  "author": "Axiom Black <tosin@axiomblack.com>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/axtos/axb-zohomail-mcp.git"
  },
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "^0.0.5",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "agents": "^0.0.80",
    "hono": "^4.6.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240620.0",
    "typescript": "^5.4.0",
    "wrangler": "^3.90.0"
  }
}
